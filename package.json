{
  "name": "jardin-verde",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered plant collection manager with personalized care plans",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for static site'",
    "lint": "npm run lint:js && npm run lint:css",
    "lint:js": "eslint api/**/*.js",
    "lint:css": "stylelint 'frontend/**/*.css' || echo 'No CSS files to lint'",
    "format": "prettier --write '**/*.{js,json,css,html,md}'",
    "format:check": "prettier --check '**/*.{js,json,css,html,md}'",
    "security:audit": "npm audit --production",
    "security:check": "npm audit --audit-level=moderate",
    "test": "echo 'Tests not yet configured'",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "stylelint": "^16.2.1",
    "stylelint-config-standard": "^36.0.0"
  },
  "keywords": [
    "plants",
    "garden",
    "ai",
    "anthropic",
    "claude",
    "plant-care",
    "vercel"
  ],
  "author": "",
  "license": "MIT"
}
